<nav class="flex items-center gap-1.5 text-[15px] text-ink-3 mb-1">
  <%= link_to t("shared.admin_dashboard"), admin_root_path, class: "font-semibold text-go" %>
  <span>/</span>
  <span class="font-semibold text-ink"><%= title %></span>
</nav>
